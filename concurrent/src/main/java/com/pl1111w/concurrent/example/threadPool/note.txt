不同的业务不应该 线程池共享 鸡蛋不能放到一个篮子里面
例如A业务不重要占据了大部分线程，业务B重要却拿不到资源
应该分成A、B 两个线程池


最佳线程数：
io密集：(io_time+cpu_time)/cpu_time * cpu_num
非io密集：1 + cpu_num

拒绝策略（前两种常用）：
1、直接抛异常（默认）
2、调用executor的线程调用run方法
3、丢弃最老的任务，最前面的任务
4、什么都不做